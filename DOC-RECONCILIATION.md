# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass, which treats
the code as read-only ground truth and edits only documentation so it reflects the repo's actual
current state. It changed no source code, config, tests, or lockfiles, and ran no builds or tests.
Its purpose is to give any reviewer a traceable record of what was found, what was changed, and
what should be reviewed by a human.

---

## Per-Claim Findings

### 1. What it is
**Status: consistent**

Evidence: `Calibrate/App/CalibrateApp.swift` (`@main` entry, ModelContainer for Question/DailySet/Answer/UserProfile),
`Calibrate/Views/Game/DailySetView.swift` (5-question carousel), `Calibrate/Models/Answer.swift`
(fields: `lower50`, `upper50`, `lower90`, `upper90`, `pointEstimate`) — all confirm the
documented description: daily iOS prediction game with 5-question rounds, dual 50%/90%
confidence-interval input, and calibration accuracy tracking. README intro and CLAUDE.md overview
match the code entry points.

### 2. Current state (phase / features)
**Status: consistent**

Evidence:
- All Phase 3 source files are present: `PremiumStore.swift`, `LeaderboardService.swift`,
  `FriendGroupView.swift`, `CalibrationCurveView.swift`, `PremiumUpgradeView.swift`,
  `Calibrate/PrivacyInfo.xcprivacy` — consistent with CLAUDE.md's "Phase 3: Complete" claim.
- README features list is accurate: StoreKit 2 subscriptions (monthly $2.99 / annual $14.99),
  global leaderboard, CloudKit sync, Swift Charts visualization, Python CLI — all verified in
  code (`Constants.StoreKit.*`, `LeaderboardService.fetchLeaderboard()`,
  `CalibrationCurveView.swift`, `scripts/question_generator.py`).
- `CalibrateTests/CalibrationEngineTests.swift` contains 15 test methods (8 mandated by the
  roadmap; the extra 7 are bonus coverage). Tests pass the roadmap's minimum spec.

No drifted claims found; the fixes applied in the previous pass (`9578078 docs: reconcile
documentation to current code state`) and the lean CLAUDE.md commit (`2003778 docs: lean
CLAUDE.md`) brought all documentation into sync with the codebase.

### 3. Stack
**Status: consistent**

Evidence:
- `project.yml:11` — `SWIFT_VERSION: "6.0"` and `project.yml:12` — `SWIFT_STRICT_CONCURRENCY: complete`
- README tech table: "Swift 6.0 (strict concurrency)" — correct.
- CLAUDE.md `## Stack`: "Swift 6.0 (strict concurrency enabled)" — correct.
- `scripts/requirements.txt`: `anthropic>=0.40.0` — consistent with "Anthropic SDK" claim.
- Python 3.12 claim: `unverifiable` — `requirements.txt` does not specify a Python version;
  the scripts use `list[dict]` annotations (3.9+) but 3.12 specifically cannot be confirmed
  from reading the code. Text left unchanged.

### 4. How to run
**Status: consistent**

Evidence: `Calibrate.xcodeproj/xcshareddata/xcschemes/Calibrate.xcscheme` exists. `project.yml`
defines the `Calibrate` target with `platform: iOS`. README instruction "Build and run the
`Calibrate` scheme on your device or simulator from Xcode" and CLAUDE.md "Build and run the
`Calibrate` scheme in Xcode on device or simulator" are both correct and consistent with the
project structure.

### 5. Known risks
**Status: consistent**

Evidence: CLAUDE.md "Scoped Gates" section constraints are reflected in the code:
- No `!` force-unwrap observed in the Swift source files reviewed.
- `CalibrateApp.swift:23` — `cloudKitDatabase: .none` (manual sync via UserService/LeaderboardService,
  not automatic SwiftData-CK sync); CloudKit public DB writes confined to `LeaderboardService`.
- `scripts/question_generator.py` reads the Claude API key via macOS Keychain (`security find-generic-password`),
  not from the Xcode project.
- All async operations use `async/await`; no Combine imports found in Swift source.

### 6. Next move
**Status: unverifiable**

CLAUDE.md "Next Recommended Move" paragraph is forward-looking operator guidance. The code
cannot confirm or deny it — left unchanged.

---

## Contradictions for Manual Review

These files are outside the editable scope (`README.md` / `CLAUDE.md` / `AGENTS.md` /
`DOC-RECONCILIATION.md` / `docs/`), so they were NOT edited. A human should apply the
one-line fixes below.

### `IMPLEMENTATION-ROADMAP.md` — file structure lists files that do not exist in the codebase

The roadmap's `File Structure` block (lines 38–91) lists several Swift files absent from the
actual source tree. These were flagged in the prior reconciliation pass and remain unfixed:

| Listed in roadmap | Status | Suggested fix |
|---|---|---|
| `Calibrate/App/AppDelegate.swift` | Missing — app uses SwiftUI lifecycle (`@main CalibrateApp`), no AppDelegate | Remove this line from the file structure |
| `Calibrate/Views/History/AnswerHistoryView.swift` | Missing — no such file in `Calibrate/Views/History/` | Remove this line, or note it as deferred |
| `Calibrate/Views/Components/CalibrationScoreBadge.swift` | Missing — score display is inlined in results/dashboard views | Remove this line, or note it as deferred |
| `Calibrate/Views/Components/StreakView.swift` | Missing — streak display is inlined | Remove this line, or note it as deferred |

Files present in the source tree but absent from the roadmap file structure (informational, no
fix required unless the roadmap is used as a reference):
`Calibrate/Views/ContentView.swift`, `Calibrate/Utilities/SeededRNG.swift`,
`Calibrate/Services/ImportService.swift`, and five extra test files
(`LeaderboardEntryTests.swift`, `SeededRNGTests.swift`).

The roadmap also states `CalibrationEngineTests.swift` contains "8 unit tests" (line 85). The
actual file has 15 test methods — the minimum spec was exceeded. No fix required, but a human
may want to update the count.

### `docs/PORTFOLIO-DISPOSITION.md` — SHA tip references are stale (informational)

The disposition was written when `origin/main` tip was `cfa444a`. Two doc-only commits have
since landed (`9578078`, `2003778`). The "Last substantive commit" (`cd0031b
fix(build): add DEVELOPMENT_TEAM for App Store signing`) is still accurate — no code commits
have occurred since the disposition was written. The SHA staleness is cosmetic; remote state
is `unverifiable` from a read-only local pass, so the disposition text was left unchanged.

---

## Footer

Generated: 2026-06-02 19:42:33 PDT
Branch: docs/truth-up-2026-06-02
HEAD reconciled against: 2003778b92b308643beec9dec564db70f1e0bb62
